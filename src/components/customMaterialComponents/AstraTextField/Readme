This maintains all the uses of TextField but changes the "filled" variant to be more like how Google Calendar looks;
basically removing the bottom bar and the extra padding at the top.
It is not recommended to use "label" with this "filled" text box. Instead use "placeholder"
